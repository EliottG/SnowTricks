{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <div class="container">
    {{ form_start(form) }}
    <button class="btn btn-primary">Modifier</button>
    {{ form_rest(form) }}
</div>
{% endblock %}
